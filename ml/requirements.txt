# S40 ML dependencies (Phases 3–4).
#
# Phase 3 added the data foundation (pandas/numpy). Phase 4 adds the
# transaction fraud model: XGBoost as the specification's recommended
# tabular model (spec §39), scikit-learn for the logistic-regression
# baseline, metrics and calibration, and SHAP for explainability (spec §14).
#
# Still deliberately absent: LightGBM (a secondary candidate with no
# technical reason to add yet), and every deep-learning/audio library —
# those belong to the voice phase.
#
# Versions below are the ones actually installed and verified.
# Install alongside the backend requirements:
#     .venv\Scripts\python.exe -m pip install -r ml/requirements.txt

pandas==3.0.5
numpy==2.5.2
scikit-learn==1.9.0
xgboost==3.4.1
shap==0.52.0
joblib==1.5.3

# Added 2026-09-04 (real-data retraining pass) for the on-device mobile
# export of s40_transaction_fraud_real — see ml/export/to_onnx.py.
onnx==1.22.0
onnxmltools==1.16.0
onnxruntime==1.29.0
skl2onnx==1.20.0

{
  "name": "s40-dev-scaffold",
  "private": true,
  "version": "0.1.0",
  "description": "S40 development orchestration helper (server.js). Not an application package — the frontend (apps/web) will get its own package.json when scaffolded.",
  "scripts": {
    "dev": "node server.js",
    "dev:seed": "node server.js --seed"
  },
  "engines": {
    "node": ">=18"
  }
}

# EDA Report — Real-Data Retraining Pass

Generated by `scripts/eda_report.py`. Every number below is measured directly from the local dataset files at the paths shown — nothing is estimated or carried over from documentation. Re-run the script to reproduce.

---

## Indian Online Scam Dataset
Source file: `D:\AVARAN\S40\ml\data\raw\indian_scam\transactions.csv`  
Raw shape: 7953 rows x 11 columns. **6753 of those are exact full-row duplicates** (the file appears to be the same ~1,200-row export concatenated with itself multiple times) — dropped before any further analysis below. Deduplicated shape: 1200 rows.
### Missingness per column
| column | % missing |
|---|---:|
| is_fraudulent | 9.00% |
| amount | 8.50% |
| customer_age | 8.08% |
| card_type | 7.08% |
| purchase_category | 6.92% |
| transaction_time | 6.83% |
| location | 6.50% |
| fraud_type | 6.50% |
| transaction_id | 6.33% |
| customer_id | 5.67% |
| merchant_id | 5.58% |

### Class balance
Labeled rows: 1092 / 1200 (91.00%). Fraud rate among labeled rows: **31.32%**.

### fraud_type distribution
| value | count |
|---|---:|
| phishing | 235 |
| scam | 226 |
| Malware | 221 |
| Identity theft | 220 |
| Payment card fraud | 220 |
| nan | 78 |

### Repeat-customer structure (CORRECTED — see note)
**On the deduplicated data, this dataset has NO repeat customers**: 1132 unique customers across 1200 rows, only 0 customer(s) with more than one transaction (0.00%). Mean transactions/customer: 1.00.

**Correction note:** an earlier pass measured this dataset *before* deduplication and reported 1,126/1,132 customers with repeat transactions — that was an artefact of the ~4x duplicated raw file (the same row group repeated under the same customer_id), not real repeat activity. Deduplicated, this dataset has the **same structural problem as PaySim**: every row is effectively a cold start, so `amount_zscore`, `recipient_frequency`, `seconds_since_last_transaction` etc. would come out null/degenerate for essentially every row here too. This changes which datasets can act as a primary training source — flagged to the user rather than proceeding on the earlier reading.

Unique merchants: 100.

### Amount distribution (labeled rows)
count=997, mean=6216.60, median=6026.48, std=3871.99, min=84.71, max=17960.98.

Mean amount by label: fraud=6456.33, legit=6108.92.

### Synthetic-construction check
The raw file being a ~4x self-concatenation (6753 exact duplicate rows out of the original total) is itself strong evidence of a constructed/exported-for-practice dataset rather than an organic transaction log. On top of that, the (deduplicated) fraud_type category counts are still close to evenly split and the fraud rate (~31%) is far above any organically-observed rate. Documented as such rather than presented as organic real-world data — see docs/DATA_STRATEGY.md addendum.

---

## IEEE-CIS Fraud Detection (evaluation-only per MODEL_DATASET_MAPPING)
Sampled first 150000 rows of `train_transaction.csv` (full file is 590,540 rows) — sampled for EDA speed only.

Fraud rate in sample: **2.65%**.

`card1` (user-id proxy) repeat rate in sample: 5875/8948 values appear more than once (65.66%). Confirms the existing MODEL_DATASET_MAPPING finding that this dataset cannot reliably support per-user behavioural baselines (card1 is a proxy, not a verified user).

Rows with a matching identity-file row: 40.93%.

### Amount distribution
mean=128.11, median=75.00, std=200.56, max=5094.95.

**Role in this pass:** evaluation-only generalization check (different fraud mechanism — card-not-present chargebacks, not P2P social engineering), never pooled into the training label. See docs/DATA_STRATEGY.md §10b and ml/datasets/preparation.py.

---

## PaySim (evaluation-only per MODEL_DATASET_MAPPING)
Sampled first 300000 rows (full file is ~6,362,620 rows) — sampled for EDA speed only.

Fraud rate in sample: **0.06%**.

`nameOrig` (sender) repeat rate in sample: 18/299982 accounts appear more than once (0.01%). Reconfirms the existing measured finding (docs/DATA_STRATEGY.md §10b: ~0.15% on the full dataset) — almost every row is a cold start for a per-user model.

`type` distribution:
| value | count |
|---|---:|
| CASH_OUT | 105401 |
| PAYMENT | 104056 |
| CASH_IN | 63408 |
| TRANSFER | 24905 |
| DEBIT | 2230 |

Fraud only occurs in: ['CASH_OUT', 'TRANSFER'].

**Role in this pass:** evaluation-only generalization check, never pooled into the training label (structural incompatibility, not a licensing issue for this one — GPL-3.0 simulator, PARTIALLY_VERIFIED Kaggle distribution).

---

## S40 Synthetic (minority blend component, 10-20% of training rows)
Generated with seed=40, users=800, history_per_user=25 (same config the existing fraud/anomaly cards use). Shape: 21247 rows.

Fraud rate: **3.77%**.

---

## Consequence for feature/column selection — REVISED FINDING

**All three real datasets available to this project (Indian Online Scam, PaySim, IEEE-CIS) turn out to lack repeat-entity history once measured correctly** (the Indian dataset's apparent repeat-customer structure was an artefact of its raw file being duplicated ~4x — corrected above). This matches the existing, broader conclusion already recorded in docs/DATA_STRATEGY.md §10b: no public/provided dataset currently available to this project can express S40's per-user behavioural feature space. `customer_id`, `merchant_id`, `amount`, `transaction_time` still map cleanly onto S40's canonical schema for the Indian dataset, and its labels are still `s40_compatible` in spirit (Indian phishing/scam/identity-theft categories) — but a model trained on it would suffer the same degenerate-feature problem PaySim was measured to have. This is reported back to the user before proceeding further, since it invalidates the primary-training-source choice the approved plan was built on.
